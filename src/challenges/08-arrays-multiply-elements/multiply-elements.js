/*
Create a function called multiplyAll(arr, multiplier) that takes an array of numbers and a multiplier, then returns a new array where each element is multiplied by the multiplier.
Example:
multiplyAll([1, 2, 3], 3)
*/
export const multiplyAll=(arr,multiplier)=>arr.map(el=>el*multiplier)