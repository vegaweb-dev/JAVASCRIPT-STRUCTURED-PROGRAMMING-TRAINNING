/*Create a function called isEmpty(arr) that receives an array and returns true if it is empty, or false if it contains elements.
Example input:
([])
Expected output:
true*/

export const isEmpty = (arr) => arr.length === 0;
